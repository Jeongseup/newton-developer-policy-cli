{
  "type": "object",
  "description": "Stable KRW Policy Parameters",
  "required": ["minimum_capital_requirements", "currency"],
  "properties": {
    "minimum_capital_requirements": {
      "type": "number",
      "description": "The minimum capital requirement in the specified currency to allow minting."
    },
    "currency": {
      "type": "string",
      "description": "The currency code (e.g., KRW) to check against the reserves."
    }
  }
}
